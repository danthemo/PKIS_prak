{
  "ConnectionStrings": {
    "ToirDb": "${TOIR_DB_CONNECTION}"
  },
  "Jwt": {
    "Issuer": "${JWT_ISSUER}",
    "Audience": "${JWT_AUDIENCE}",
    "SigningKey": "${JWT_SIGNING_KEY}"
  },
  "Logging": {
    "Level": "${LOG_LEVEL}"
  },
  "Cors": {
    "AllowedOrigins": "${CORS_ALLOWED_ORIGINS}"
  },
  "Observability": {
    "ServiceName": "toir-api",
    "Environment": "${ASPNETCORE_ENVIRONMENT}"
  }
}

